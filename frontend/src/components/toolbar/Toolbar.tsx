import React from 'react'

import './Toolbar.scss'

function Toolbar () {
  return (
    <div className='l-toolbar toolbar-container'>
      Toolbar
    </div>
  )
}

export default React.memo(Toolbar)
